# Core
pandas>=2.2
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
pyarrow>=15.0
joblib>=1.4

# Vietnamese word segmentation (see src/vietjobs/vitext.py, section 2.4).
# Optional at import time: vitext falls back to syllable-level text if absent,
# so the pipeline still runs — the ablation just cannot be measured.
underthesea>=6.8

# Second segmenter, so "would a different segmenter rescue step 2.4?" is a
# measurement rather than an opinion. Selected with VIETJOBS_SEGMENTER=pyvi.
# Also optional: without it only scripts/segmenter_ablation.py stops working.
pyvi>=0.1.1

# Gradient boosting. LightGBM ships the salary regressor and consumes
# scipy.sparse directly; both it and XGBoost also run on the classification
# comparison axis (see models.py, CLASSIFIERS).
lightgbm>=4.3
xgboost>=2.0

# Dev
pytest>=8.0
