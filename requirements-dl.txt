# Phụ thuộc cho gói src/vietjobs/dl/ — cài vào một môi trường RIÊNG.
#
# Lý do tách khỏi requirements.txt: torch không phát hành bánh xe cho
# macOS x86_64 sau bản 2.2.2, và không có bản nào cho Python 3.14 — trong khi
# phần còn lại của dự án chạy trên 3.14. Trên máy hiện tại:
#
#   /usr/bin/python3 -m venv .venv-dl          # Python 3.9.6
#   .venv-dl/bin/pip install -r requirements-dl.txt
#   PYTHONPATH=src .venv-dl/bin/python -m vietjobs.dl.encode --task category
#
# Trên Colab/Kaggle (có GPU) thì bỏ ghim phiên bản torch.

torch==2.2.2            # bản cuối còn bánh xe macOS x86_64
transformers>=4.38,<4.47
numpy<2                 # torch 2.2.2 dựng trên numpy 1.x
pandas>=2.0
pyarrow>=15.0
scikit-learn>=1.3       # dùng lại evaluate.py để metric so được với đường học máy
